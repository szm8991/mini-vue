export function createRenderer(options:any) {
    function patch(n1,
        n2,
        container = null){

    }
    function render(vnode, container){
        
    }
    return {render}
}